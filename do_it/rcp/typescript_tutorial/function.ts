function sum(num1: number, num2: number): number {
  return num1 + num2;
}

console.log(sum(10, 20)); // 30
// sum(10, 20, 30); // error, too many parameters
// sum(null); // error, too few parameters
